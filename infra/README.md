# Infrastructure

Local and deployment infrastructure assets.

## Planned Areas

- `compose`: local development Docker Compose stack.
- `docker`: Dockerfiles and image build assets.
- `github-actions`: reusable CI/CD notes or workflow fragments.
- `observability`: logging, metrics, tracing, and dashboard assets.
