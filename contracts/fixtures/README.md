# Contract Fixtures

Use `valid/` and `invalid/` sample packages/configurations for schema and compatibility tests.

The practical MVP includes a built-in `field_ops` module/plugin fixture at `valid/v1/module-manifest-field-ops.json`. It declares the reference plugin surfaces, permissions, template form/app references, runtime compatibility, and sync handler used by the demo seed and package path.
