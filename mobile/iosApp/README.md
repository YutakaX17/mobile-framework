# iOS App

Placeholder for the iOS application entry point. The shared Kotlin runtime and Compose UI modules are scaffolded first; native iOS project files will be added when the runtime targets are exercised in CI.
